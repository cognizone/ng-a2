{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "outDir": "./out-tsc/spec",
    "types": ["jest", "@types/jest", "node"] // Ensure Jest types are included
  },
  "include": ["**/*.spec.ts", "**/*.d.ts", "src/**/*.spec.ts", "src/**/*.d.ts"]
}

# A2-UI

This library was generated with [Angular CLI](https://github.com/angular/angular-cli) version 20.

## Code scaffolding

Run `ng generate component component-name --project a2-ui` to generate a new component. You can also use `ng generate directive|pipe|service|class|guard|interface|enum|module --project a2-ui`.

> Note: Don't forget to add `--project a2-ui` or else it will be added to the default project in your `angular.json` file.

## Build

Run `npm run build:a2-ui` to build the project. The build artifacts will be stored in the `dist/` directory.

## Publishing

After building your library with `ng build a2-ui`, go to the dist folder `cd dist/a2-ui` and run `npm publish`.

## Running unit tests

Run `npm test a2-ui` to execute the unit tests via [Jest](https://jestjs.io/).

## Further help

To get more help on the Angular CLI use `ng help` or go check out the [Angular CLI Overview and Command Reference](https://angular.io/cli) page.

## Key Features

- **Are You Sure Modal**: Confirmation dialog component with customizable header, text, and action buttons
- **Global Alert Service**: Snackbar notification service with error, warning, info, success, and debug variants
- **Loading Stairs**: Loading indicator component with optional message display and dynamic updates
